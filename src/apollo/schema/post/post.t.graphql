type Query {
    posts(categoryId: String, page: Int, limit: Int, orderType: String): Page
    post(id: ID!): Post!
    searchPostsByTag(tag: String): [SimplePost!]!
    bookmarkedPosts: [BookmarkedPost!]!
}

type Mutation {
    createNewPost(categoryId: String, title: String, content: String, tags: [String], option: PostOptionInput): PostCreateResponse
    createLocalPosts(newLocalPosts: [NewLocalPost]): LocalPostCreatesResponse
    deletePost(id: ID!): Boolean
    deletePosts(ids: [ID!]): Boolean
    updatePost(id: ID!, title: String content: String, tags: [String], option: PostOptionInput): PostCreateResponse
    changePostsCategory(categoryId: String, ids: [ID!]): Boolean
    mailPosts(ids: [ID!]): Boolean
}

input NewLocalPost {
    title: String
    content: String
    localFormedDateString: String
}

input PostOptionInput {
    hasAutoTag: Boolean
}

type Page {
    data: [SimplePost!]!
    total: Int!
    currentPage: Int!
    limit: Int!
    hasNext: Boolean!
}

type SimplePost {
    id: ID!
    title: String
    preview: String!
    tags: [String!]
    autoTags: [String!]
    createdAt: String!
    bookmarked: Boolean!
}

type Post {
    id: ID!
    categoryId: String
    title: String
    content: String!
    tags: [String!]
    createdAt: String
    option: PostOption
    bookmarked: Boolean!
}

type PostOption {
    hasAutoTag: Boolean
}

type BookmarkedPost {
    id: ID!
    documentId: String!
    title: String
    preview: String!
}

type PostCreateResponse {
    success: Boolean!
    id: ID!
}

type LocalPostCreatesResponse {
    success: Boolean!
    ids: [ID!]
}
